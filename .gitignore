.env
__pycache__/
node_modules/
.next/
dist/
*.db
*.sqlite
*.bak
*.bak-*

# Python / Virtualenv
.venv/
*.py[cod]
.pytest_cache/

# IDEs & System files
.idea/
.vscode/
.claude/
.codex/
.impeccable/
.DS_Store
Thumbs.db

# Agent / WorkBuddy memory & local state (never commit)
.workbuddy/

# Change-tracking workbook backups (守则 4 排除项)
docs/*_备份.xlsx

# Runtime data (bridges workdir, sqlite, generated task dirs) — never commit
data/
bridge/.cursor_client_state.json
bridge/*_state.json

# Test & tooling artifacts
backend/.pytest-temp/
docs/.archify-delivery-*/

# Archify architecture diagram generated artifacts (visual-check / spec / html)
# 说明：工具产物可再生；docs/architecture-dark.png/svg 为已入库的正式架构图。
docs/agent-console-architecture.*

# Personal materials (personal resume, not project docs)
docs/简历-*.md
