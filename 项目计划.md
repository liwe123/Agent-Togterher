# 项目计划

## 1. 项目目标

将当前“本地优先的多智能体运控台”逐步演进为一个**可扩展、可治理、可观测、可多团队协作**的任务执行平台。

### 终局目标
- 支持稳定的任务调度与恢复
- 支持多 Agent 协同与可编排工作流
- 支持 PostgreSQL、多 Worker、跨进程事件总线
- 支持任务审计、回放、成本统计与可观测性
- 支持多租户、权限、配额与插件生态

---

## 2. 当前架构现状

当前系统已经具备以下能力：

- 前端使用 Next.js App Router 构建控制台、群聊、任务、通讯录、设置页面
- 后端使用 FastAPI 提供 REST API、WebSocket 和任务执行入口
- `MessageHub` 负责接收用户消息、识别 `@Agent` 提及并生成任务
- `AgentOrchestrator` 负责任务执行、模型调用、工具调用、状态更新与事件广播
- 数据层使用 SQLite + SQLAlchemy Async
- 模型层通过 LiteLLM 统一接入多家模型供应商

### 当前阶段的优点
- 开发速度快
- 本地可运行
- 交互链路完整
- 实时反馈能力较强

### 当前阶段的主要限制
- 任务执行依赖进程内调度
- SQLite 不适合高并发写入
- 编排器职责过重
- 多实例部署与横向扩展困难
- 可观测性和任务治理能力仍偏弱

---

## 3. 技术架构演进方向

### 阶段 1：稳定化
目标是把“能跑”变成“稳跑”。

重点工作：
- 引入 PostgreSQL
- 统一任务状态机
- 抽离任务执行服务
- 建立基础日志、指标和 trace id 规范

### 阶段 2：平台化
目标是把“聊天触发执行”升级成“可治理的任务平台”。

重点工作：
- 引入持久化任务队列
- 独立 Worker 进程消费任务
- 支持任务重试、超时、死信、优先级
- 拆分编排器职责
- 统一消息入口与任务入口

### 阶段 3：分布式化
目标是把系统从“单进程内协作”升级为“可跨实例协作、可水平扩展、可恢复”的分布式执行平台。

#### 3.1 阶段定位
P3 的核心不是单纯增加实例数量，而是先把进程内耦合的实时事件链路、任务执行链路和连接管理链路拆开，形成可独立扩展的基础设施层。只有当消息分发、任务执行、状态同步和故障恢复都具备明确边界后，系统才具备真正的多实例能力。

#### 3.2 重点工作
- 引入 Redis Pub/Sub 或 NATS 作为跨进程事件总线，统一承载任务状态变更、消息广播、心跳与系统通知
- 将 WebSocket 连接管理与事件分发解耦，避免连接层直接依赖业务内存状态
- 建立事件规范，明确事件类型、payload 结构、幂等键、顺序要求与过期策略
- 将 Worker 运行方式改为集群化部署，支持多个 Worker 实例并行消费任务
- 建立任务租约、续租与失联回收机制，避免实例宕机后任务永久挂起
- 增加分布式场景下的状态对账与重放能力，保证消息、任务与前端展示最终一致
- 为后续弹性扩缩容预留实例注册、健康检查和流量感知能力

#### 3.3 具体拆解
- **事件总线层**：定义统一事件模型，接入发布 / 订阅、重试、死信与告警能力
- **连接网关层**：负责 WebSocket 长连接、用户会话映射、断线重连与消息投递
- **任务执行层**：Worker 只关注任务领取、执行、回传结果，不直接处理前端连接
- **状态同步层**：统一处理任务状态、消息状态、Agent 状态的跨进程广播与落库
- **治理层**：补充限流、幂等、失败隔离、审计记录和恢复策略

#### 3.4 里程碑
- **P3-M1**：完成事件总线方案选型与事件协议草案
- **P3-M2**：完成 WebSocket 连接层与业务事件分发解耦
- **P3-M3**：完成 Worker 集群化与任务租约回收机制
- **P3-M4**：完成跨实例消息广播、状态一致性验证与故障演练

#### 3.5 验收标准
- 单个实例重启后，未完成任务可由其他实例继续接管
- 多个 Worker 同时运行时，任务不会被重复消费或重复完成
- WebSocket 断线重连后，前端可重新同步最新任务状态与消息状态
- 事件广播在跨进程场景下仍能稳定送达，且可定位丢失原因
- 能通过健康检查、日志与指标快速判断某个实例是否异常

#### 3.6 风险与应对
- **风险**：事件重复投递导致前端显示重复消息
  - **应对**：统一幂等键与去重策略，前端按事件 ID 合并渲染
- **风险**：事件总线引入后排障复杂度上升
  - **应对**：保留 trace id / task id / conversation id 全链路关联
- **风险**：Worker 扩容后出现抢占冲突
  - **应对**：通过租约、原子 claim 和超时回收控制并发边界
- **风险**：连接层和事件层解耦后状态同步滞后
  - **应对**：在重连、定时心跳和关键状态变化时强制做一次快照同步

#### 3.7 阶段产出
- 分布式事件总线
- 多实例 WebSocket 推送链路
- Worker 集群化运行方案
- 跨进程状态同步与恢复机制
- 分布式部署下的排障与验收基线

### 阶段 4：产品化
目标是让系统可被团队长期使用。

重点工作：
- 多租户与权限体系
- 审计与回放能力
- 成本、配额、限流与策略中心
- 插件市场与工作流模板

---

## 4. 阶段里程碑与达成现状

| 阶段 | 目标 | 关键成果 | 状态 | 完成标志 |
|---|---|---|---|---|
| Phase 0 | 现状梳理 | 模块职责、数据流、风险清单 | ✅ 已达成 | 能完整描述当前系统链路 |
| Phase 1 | 稳定化 | 统一状态机、任务执行解耦、AST 工具沙箱 | ✅ 已达成 | 后端重启不丢任务，支持更稳定并发 |
| Phase 2 | 平台化 | 持久化队列、独立 Worker、任务治理 | ✅ 已达成 | 任务可排队、可重试、可限流 |
| Phase 3 | 分布式 | 事件总线 (Redis Pub/Sub)、多实例 WebSocket 解耦 | ✅ 已达成 | 多机部署不影响实时推送 |
| Phase 4 | 产品化 | 用户认证、RBAC 多租户、审计、成本、配额、回放、插件注册、工作流模板 | ✅ 已达成 | 18 张表完整，多团队正式投产使用 |
| Phase 5 | 生态与策略 | 深度策略中心、更多预设插件与第三方协同适配 | ⏳ 规划中 | 平台可持续扩展新能力 |

---

## 5. 按月实施路线图

### 第 1 个月：架构盘点与治理基线

**目标**：梳理当前系统，建立统一的技术和治理基线。

**任务**
- 梳理后端模块职责边界
- 画出任务执行主链路
- 定义统一任务状态机
- 定义 trace id / correlation id 规范
- 输出风险清单与演进边界图

**交付物**
- 架构说明文档
- 状态机草案
- 风险清单

---

### 第 2 个月：数据库迁移准备

**目标**：为 PostgreSQL 迁移做充分准备。

**任务**
- 梳理 SQLite 限制与兼容点
- 检查事务、锁和并发写入点
- 预留 Alembic 迁移支持
- 设计数据库初始化与迁移方案

**交付物**
- 数据库迁移方案
- Schema 版本管理方案

---

### 第 3 个月：任务执行解耦

**目标**：让 API 与执行流程分离。

**任务**
- 抽离 `TaskService`
- 让 `MessageHub` 只负责消息转任务
- 统一任务 claim / renew / complete / fail 的入口
- 降低 `AgentOrchestrator` 直接耦合

**交付物**
- 任务服务层
- 更清晰的职责边界

---

### 第 4 个月：PostgreSQL 上线

**目标**：完成数据库切换。

**任务**
- 切换到 PostgreSQL
- 验证启动、迁移、恢复逻辑
- 复测任务并发、消息写入、WebSocket 广播
- 清理 SQLite 顺序执行假设

**交付物**
- PostgreSQL 正式可用
- 并发能力提升

---

### 第 5 个月：持久化队列设计

**目标**：建立可靠任务调度基础。

**任务**
- 设计任务队列表
- 定义优先级、重试次数、超时、租约
- 设计 worker 领取机制
- 设计死信与失败任务处理

**交付物**
- 队列模型
- worker 消费协议
- 任务重试策略

---

### 第 6 个月：独立 Worker 化

**目标**：把执行从 API 服务中剥离。

**任务**
- 增加独立 worker 进程
- 让 worker 从队列消费任务
- 引入任务租约和并发控制
- 支持 worker 热重启

**交付物**
- API 与 Worker 分离
- 执行与接入解耦

---

### 第 7 个月：可观测性体系

**目标**：让系统看得见、查得清。

**任务**
- 统一日志格式
- 打通 task id / trace id / conversation id
- 增加任务耗时、模型成本、失败率指标
- 增加工具调用统计

**交付物**
- 可观测性基线
- 运维排障能力

---

### 第 8 个月：任务回放与审计

**目标**：让系统可复盘。

**任务**
- 保存任务执行快照
- 支持按 step 回放
- 支持从失败节点恢复
- 记录关键决策点和模型输出

**交付物**
- 审计面板
- 回放能力

---

### 第 9 个月：事件总线化

**目标**：支持多实例实时协作。

**任务**
- 引入 Redis Pub/Sub 或 NATS
- 解耦 WebSocket 连接层与事件分发层
- 保证跨进程事件一致性

**交付物**
- 分布式实时事件通道

---

### 第 10 个月：模型网关化

**目标**：提升模型调度和成本控制能力。

**任务**
- 增强 provider 路由
- 做模型 fallback、熔断、限流
- 按任务类型分模型
- 引入成本预算和模型策略

**交付物**
- 模型网关
- 成本控制策略

---

### 第 11 个月：多租户与权限

**目标**：让系统可以给团队正式使用。

**任务**
- 建立用户体系
- 增加 workspace 权限
- 增加 Agent 配置权限
- 增加任务查看 / 执行权限
- 增加配额控制

**交付物**
- 权限系统
- 多租户基础能力

---

### 第 12 个月：工作流与插件生态

**目标**：进入平台化阶段。

**任务**
- 引入工作流模板
- 支持条件分支
- 支持人工审批节点
- 建立插件注册中心
- 做工具版本管理

**交付物**
- 流程模板
- 插件/工具生态基础

---

## 6. 优先级建议

### 第一优先级
- PostgreSQL 迁移
- 任务执行解耦
- 统一状态机
- 基础可观测性

### 第二优先级
- 持久化队列
- 独立 Worker
- 审计与回放
- 模型网关化

### 第三优先级
- 事件总线
- 多租户权限
- 插件市场
- 工作流模板

---

## 7. 风险与应对

### 风险 1：任务执行耦合过深
**应对**：尽早拆分服务边界，先做接口稳定再做实现替换。

### 风险 2：SQLite 并发瓶颈
**应对**：优先迁移 PostgreSQL，避免在原数据库上做过多补丁。

### 风险 3：实时事件在多实例下失效
**应对**：提前规划事件总线，不要把 WebSocket 事件直接绑定进程内内存状态。

### 风险 4：编排器越来越难维护
**应对**：拆分职责，建立单元测试与契约测试。

### 风险 5：可观测性不足导致排障困难
**应对**：从一开始统一 trace id、task id 和结构化日志。

---

## 8. 成功标准

如果项目按计划推进，12 个月后的系统应满足：

- 任务可以排队、重试、超时、恢复
- 多 Agent 编排流程可配置
- 多实例部署下实时事件仍然可靠
- 所有关键动作可审计、可回放、可追踪
- 支持多团队、多 workspace 使用
- 模型调用成本和性能可以持续优化

---

## 9. 总结

项目的长期方向不是单纯“加功能”，而是把当前的聊天驱动 Agent 系统，升级成一个真正的**可编排任务平台**：

- 从进程内执行，升级为持久化调度
- 从单机 SQLite，升级为 PostgreSQL + Worker
- 从“能跑”，升级为“可治理、可观测、可扩展”
- 从单人使用，升级为团队协作平台

