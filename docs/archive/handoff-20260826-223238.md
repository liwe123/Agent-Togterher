# Handoff 2026-08-26 22:32

> 范围：全面审查后的「欠账清理」批次（第一批 BUG + 第二批既有 PRD 收尾）。
> 进度：11 项计划中 8 项已完成并提交，3 项未完成（C-164、C-165、验收与推送）。
> 测试基线：后端 168 passed；前端 build / test / lint 全绿。

---

## 一、本批已完成（8 个提交，未推送）

| 提交 | ID | 类型 | 内容 |
|---|---|---|---|
| 7768e56 | C-155 | BUG | cost_stats 日趋势按方言选日期函数，修复 PG 下 strftime 报错 |
| c1d6dbb | C-158 | BUG | WebSocket 支持 JWT 凭证认证 + `ws_allowed_origins` Origin 白名单（默认空=不校验） |
| 5d5a7cb | C-157 | Optimization | 删除 orchestrator 重复定义的 `_build_task_context_messages` 死代码（60 行） |
| dfc3dcc | C-159 | BUG | REST 老路由 tasks/agents/provider_keys/custom_models 补齐 RBAC（条件守卫 `app/api/rbac_compat.py`：有 JWT 才校验，静态 token/开放模式保持兼容） |
| e140d7c | C-156/C-161 | BUG | 前端定时器泄漏修复 + 全站工作区选择统一（`lib/active-workspace.ts` 共享工具，`/api/v1/workspaces/my` + localStorage + 自定义事件通知，首页/群聊/任务页联动切换） |
| 36a0c21 | C-162 | Requirement 收尾 | 配额硬熔断+限流接入执行链路（message_hub 派发与 workflows.run 两处拦截，429；速率限制为进程内滑动窗口，分布式需 Redis 的 TODO 已标注） |
| 8454f84 | C-163 | Requirement 收尾 | 插件工具注入 Function Calling 循环 + `register_plugin_tool_executor` 可扩展执行钩子（无 executor 时诚实返回 not implemented，不伪造） |
| 089be34 | C-160 | BUG | refresh token 服务端记录（`refresh_tokens` 表 + Alembic 迁移 a1b2c3d4e5f6）+ logout 真实吊销；无 jti 的历史 token 过渡期仍可用 |

**新增文件**：`frontend/src/lib/active-workspace.ts`、`backend/app/api/rbac_compat.py`、`backend/app/models/refresh_token.py`、`backend/alembic/versions/a1b2c3d4e5f6_*.py`、4 个新测试文件。

---

## 二、未完成事项（后续步骤）

### 1. C-164：FR8 WS 轨迹推送 + FR9 双层视图（PRD-单任务上下文连续性 遗留）
- 曾 3 次派子 Agent 未产出（返回空），**建议主 Agent 亲自实现或换代理工具**。
- 后端：orchestrator 轨迹写入点广播 `task.trace_updated` 事件（payload 轻量/增量）；
- 前端：task-detail 订阅事件 + 「开发者/用户」双层视图切换（用户视图隐藏 token/成本/payload）。

### 2. C-165：Provider Key 信封加密（Requirement，**需写 PRD 并三处同步**）
- 现状：`provider_credentials.api_key` 明文存库；`requirements.txt` **无 cryptography 依赖**（本次已核实），选型待定：
  - 方案 A：引入 `cryptography` 用 Fernet（需改 requirements + Docker 重建 + 本地 venv 安装）；
  - 方案 B：stdlib 自实现 AES 不可行，PBKDF2+XOR 强度不足不建议；
  - 主密钥建议复用 `jwt_secret_key` 派生（信封加密字段存 `v1:` 前缀 + 密文，兼容存量明文渐进迁移）；
- 需新增 Alembic 迁移（列加宽或加 `encrypted` 标记列），litellm_service 解密点同步改。

### 3. 验收与推送（守则 3/4）
- 派独立验收子 Agent：核对 8 个提交与《改动表》对齐、测试无回归、提交格式；
- 重跑 `python docs/generate_change_log.py` 与 `python docs/build_prd_html.py`（**必须先提交特性再跑脚本**，否则本次 commit 不入表）；
- 通过后 `git push`（远端 origin = liwe123/Agent-Togterher，main）。**当前 8 个提交均未推送。**

### 4. 文档滞后回写（Optimization，不写 PRD）
- `HANDOFF.md` 仍写「18 张表 / 106 测试」，实际 19+1=20 表（新增 refresh_tokens）、168 passed；
- `PRD-PostgreSQL迁移与Alembic迁移治理.md`、`PRD-外部节点群聊派发与Cursor桥接.md` 头部仍标「待实施」，代码已完成；
- 《项目计划.md》§2 仍写「数据层 SQLite」，实际生产已切 PG；
- PRD-PostgreSQL 开放问题 Q1（PG 凭证策略）、Q2（构建期是否跑迁移）未决。

---

## 三、环境与操作注意事项（踩坑记录）

1. **pytest 必须显式设** `$env:DATABASE_URL='sqlite+aiosqlite:///./test.db'` 且 `$env:PYTHONPATH='E:\Agents\backend'`，否则 lifespan 连 PG 失败 / import 失败。
2. **子 Agent 派发前端任务连续 3 次返回空结果、0 改动**（use-tasks.ts 等前端任务、auth 任务均如此），前端修复最终由主 Agent 完成——后续派发建议改用 explore 先探路或明确要求「先读文件再报告所见」。
3. 本机 venv 曾缺 alembic（lifespan 建表失败），已按 requirements 补装。
4. Windows 下 pytest 有 WinError 145 临时目录清理 warning，无害。
5. 5 个未跟踪文档/图片（2 份调研 md、3 份历史 handoff、2 张 png）去留待定，提交时禁 `git add -A`。
6. 改动类型字面量必须 `BUG` 全大写；类型判错改 `generate_change_log.py` 的 CURATED 后重跑，勿手改 xlsx。

---

## 四、当前数据库结构变化提示

- 新增表 `refresh_tokens`（id/user_id/jti/expires_at/revoked_at/created_at），`test_database.py` 表清单断言已同步更新。
- `ws_allowed_origins` 配置默认空（不校验），生产建议与 `cors_origins` 同值。
- 配额限流为进程内计数；插件工具真实 Webhook 转发未实现（仅有钩子）——均为刻意边界，见对应提交。
