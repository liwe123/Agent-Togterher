# Handoff: PostgreSQL 迁移与 Alembic 迁移治理（实施进行中，代码半成品未提交）

> 生成时间：2026-08-20 22:20  
> 工作目录：`E:\Agents`  
> 远程：`https://github.com/liwe123/Agent-Togterher.git`（默认分支 `main`）  
> 本文件定位：**本次会话进度 + 当下处境 + 后续任务**。架构全景 / 18 张表 / Phase 4 批次 / 运行命令见根目录 `HANDOFF.md`（evergreen，勿重复搬运）。上一份接力：`docs/handoff-20260820-213413.md`。

---

## 1. Current state（本次进度）

### 已完成：上份 handoff 的任务 1 与任务 3

1. **Docker 容器真跑 `backend/tests/test_external_mention.py`——3/3 全部通过** ✅
   - 方法：`docker cp` tests 进正在运行的 `agents-backend-1` 容器 + `pip install --target /tmp/pytestdeps pytest pytest-asyncio httpx` + `PYTHONPATH=/tmp/pytestdeps:/app MODELS_CONFIG_PATH=/app/config/models.yaml python -m pytest`
   - 结果：路由到集成节点 / 桥接成功 / 桥接超时三个用例全部 PASSED，端到端坐实。
   - 注意：容器内的 `/app/tests` 与 `/tmp/pytestdeps` 是临时的，容器 down/recreate 后消失（可按上述命令重放）。

2. **工作区遗留未提交文件处置** ✅
   - `docs/architecture-dark.png/.svg`：判定为**误删**（README 第 27-29 行仍引用深色架构图），已 `git checkout HEAD --` 恢复，断链已修复。
   - 4 个未跟踪调研材料（`docs/Cursor、OpenAI Codex 与 Google Antigravity 的 Agent 桥接调研报告.txt`、`docs/mvp_architecture.png`、`docs/reference_bridge_architecture.png`、`docs/自建多 Agent 协作项目：可行性评估与 MVP 路线.txt`）：均为上一轮 Cursor 桥接调研的参考材料（Manus AI 生成），**按范围纪律不纳入提交**，保留在工作区，最终去留待用户定夺。

### 进行中：上份 handoff 的任务 4——PostgreSQL 迁移与 Alembic（Requirement，PRD 已写，代码改了一半，未提交未测试）

**类型判定：Requirement**（架构治理基线 R-02/R-07 的落地，Phase 0 演进链底座）。PRD 已按守则②三处同步中的两处完成：

- ✅ `docs/prd/PRD-PostgreSQL迁移与Alembic迁移治理.md`（含目标/用户故事/FR1-FR6/AC1-AC8/数据模型/里程碑 M1-M6/风险/系统关系/开放问题 Q1-Q2）
- ✅ `docs/PRD.md` 的「PRD 文档索引」已登记该行（关联提交/追踪表暂填 —，提交后可更新）
- ⏳ `PRD.html`：由 `docs/build_prd_html.py` 生成，**等提交后重跑脚本时自动同步**，无需手改

**已完成的代码改造（全部在工作区，未提交）：**

| 文件 | 状态 | 改动要点 |
|---|---|---|
| `backend/app/core/config.py` | 修改 | `database_url` 默认值改 `postgresql+asyncpg://agent:agent@db:5432/agent_console` |
| `backend/app/db/session.py` | 重写 | PG 下引擎加连接池（pool_size=10/max_overflow=20/pool_pre_ping/pool_recycle=1800）；`init_db()` 改为 `asyncio.to_thread` 执行 `alembic upgrade head`（alembic.command API）；**已删除** `create_all` 与 `_migrate_sqlite_schema` 手写补列；SQLite PRAGMA 仍按 scheme 条件注册 |
| `backend/requirements.txt` | 修改 | 新增 `alembic>=1.13,<2.0`、`asyncpg>=0.29,<1.0` |
| `backend/Dockerfile` | 修改 | 新增 `COPY backend/alembic ./alembic` 与 `COPY backend/alembic.ini ./alembic.ini` |
| `docker-compose.yml` | 修改 | 新增 `db` 服务（postgres:16-alpine、POSTGRES_USER/PASSWORD/DB 环境变量、pg_isready healthcheck、`postgres_data` volume）；backend `DATABASE_URL` 改 `${DATABASE_URL:-postgresql+asyncpg://agent:agent@db:5432/agent_console}`；backend `depends_on` 加 `db: service_healthy`；顶层 volumes 加 `postgres_data` |
| `backend/alembic.ini` | 新增 | `script_location = %(here)s/alembic`（ini 目录相对解析，不依赖 cwd）；URL 占位符由运行时注入 |
| `backend/alembic/env.py` | 新增 | async 模板（`async_engine_from_config` + `run_sync`），支持 sqlite+aiosqlite 与 postgresql+asyncpg；URL 优先取注入值，否则回落 `get_settings().database_url`；`render_as_batch`（sqlite 方言）；sys.path 注入 backend 根使 `app.*` 可导入 |
| `backend/alembic/script.py.mako` | 新增 | 标准迁移脚本模板 |
| `backend/alembic/versions/` | 空目录 | **基线迁移脚本尚未生成** |

---

## 2. 当下处境与风险（接力 Agent 必读）

1. **工作区是半成品，代码能改的都改了，但一步都没验证**：
   - `alembic/versions/` 为空 → 基线迁移未生成 → `init_db()` 的 `upgrade head` 是无操作 → 起服务后表不会创建、seed 会崩。
   - 改动尚未跑过任何测试（pytest / 前端 build 均未执行）。
   - `docker-compose.yml` 已切 PG，但**没有重建容器验证过**；当前在跑的 `agents-backend-1` 仍是旧镜像（sqlite），不受工作区影响。
2. **依赖安装卡壳（本次会话未解决的唯一阻塞）**：
   - 隔离 venv `~/.workbuddy/binaries/python/envs/default` 需装 `backend/requirements-dev.txt` + alembic + asyncpg，**全部失败**。
   - 症状：直连 pypi 出现 `Read timed out` 重试与一次诡异的 `uvicorn versions: none`（但 `pip index versions uvicorn` 能列出全部版本，矛盾）；清华镜像 `IncompleteRead` 断流；还出现过一次 `ResolutionImpossible`，**其关键冲突描述被 `tail -8` 截断未看到，未完成诊断**。
   - 已排除：无代理（env/WinHTTP/pip.ini 均干净）、venv 无既有包冲突（pip list 干净）。
   - 下次继续：先重跑 `pip install ... 2>&1 | grep -B5 -A15 "ResolutionImpossible\|conflict"` 看完整冲突段落；若确是版本冲突再拆开装；若仍是网络问题，考虑换阿里镜像 `https://mirrors.aliyun.com/pypi/simple/` 或逐包安装。
3. **验证策略已在 PRD 定好**（AC1-AC8）：空 PG 上 `upgrade head` 建全 19 表、autogenerate 无遗漏、`downgrade base` 可回退、全量测试通过、docker compose up 端到端健康。
4. **当前 `backend/tests` 无 conftest.py**：25 个测试文件各自 `create_async_engine` + `create_all` 自建临时 sqlite，迁移改造后测试仍可用 sqlite 跑（PRD FR5/FR6 策略），本轮计划新增 conftest.py 共享 fixture + `test_alembic_migrations.py`。

---

## 3. Next steps（后续任务 · 按优先级）

1. **解决 venv 依赖安装**（见上节第 2 条的诊断思路），装齐 `requirements-dev.txt` + alembic + asyncpg。
2. **生成基线迁移**：用临时空 sqlite 库跑 `alembic revision --autogenerate -m "baseline"`（DATABASE_URL 指向临时 sqlite，如 `sqlite+aiosqlite:///./tmp_autogen.db`），生成后人工核对 19 张表无遗漏，再删除临时库。PRD FR1/AC2/AC3。
3. **补齐剩余实施**（PRD FR4/FR5）：
   - `backend/scripts/migrate_sqlite_to_pg.py` 数据迁移脚本（--dry-run 预检 + 行数校验）；
   - `backend/tests/conftest.py` 共享 fixture（db_engine/db_session）；
   - `backend/tests/test_alembic_migrations.py`（upgrade 建表/downgrade 回退/autogenerate 无遗漏）；
   - 示范改造 `test_external_mention.py`、`test_database.py` 用 conftest。
4. **全量验证**（守则③）：`pytest backend/tests` 全绿 + 前端 `npm run build`+`npm test`+`npm run lint` + `docker compose up --build` 端到端（db healthy → backend 自动 alembic upgrade → health 通过）。
5. **收尾流程**（守则①③④）：重跑 `docs/generate_change_log.py` + `docs/build_prd_html.py` → 派独立子 Agent 验收（只校验不修改：改动表中文无误/PRD 三处同步/测试无回归/文档与 git 一致）→ 验收通过后 Conventional Commits 提交（建议 `feat: 引入 Alembic 迁移治理并将数据库迁移至 PostgreSQL`）→ `git push origin main`。**严守 `git add` 仅加真实改动文件（见下节清单），绝不 `-A`**。
6. **宿主 `bridge/cursor_client.py` 与 Cursor IDE 真实联动 E2E**（上份 handoff 任务 2，仍未动；需本机 Cursor 在 `E:/cursor/resources/app/bin/cursor`）。
7. **4 个调研材料的最终去留**：本轮判定不纳入提交，用户可决定移出 docs/ 或删除。
8. **再次接力**：重跑 handoff 流程。

### 本特性提交时的 git add 清单（半成品全量）

```
backend/Dockerfile  backend/app/core/config.py  backend/app/db/session.py
backend/requirements.txt  docker-compose.yml  docs/PRD.md
backend/alembic.ini  backend/alembic/  docs/prd/PRD-PostgreSQL迁移与Alembic迁移治理.md
+ 尚待新增：基线迁移脚本、scripts/migrate_sqlite_to_pg.py、tests/conftest.py、tests/test_alembic_migrations.py、改造后的两个示范测试
+ 提交与重跑脚本后：docs/PRD.md（CHANGELOG 再生）、docs/Agent_Console_变更追踪.xlsx、PRD.html、（可能）docs/generate_change_log.py 的 CURATED 条目
勿加：docs/*调研*.txt、docs/mvp_architecture.png、docs/reference_bridge_architecture.png、docs/handoff-*.md（按惯例 handoff 文档不提交）
```

---

## 4. Relevant artifacts（引用，勿内联）

- `docs/prd/PRD-PostgreSQL迁移与Alembic迁移治理.md` — **本特性 PRD（权威需求源，含 FR/AC/里程碑）**
- `docs/架构治理基线.md` — R-02/R-07 风险与演进边界（本迁移的架构依据）
- `docs/Agent任务守则.md` — 4 守则（改动表/类型判定/测试验收/推送规范）
- `HANDOFF.md`（根目录）— 架构全景 / 18 张表 / 运行命令
- `docs/handoff-20260820-213413.md` — 上一份接力（外部节点派发与 Cursor 桥接）
- `backend/alembic/env.py`、`backend/app/db/session.py` — 已完成改造的核心文件
- 隔离 venv：`~/.workbuddy/binaries/python/envs/default`（已装 openpyxl/markdown，**缺 pytest/alembic 等后端依赖**）

## 5. Suggested skills（按需加载）

- `tdd` — 继续实施 PostgreSQL 迁移时配合 `test_alembic_migrations.py` 先写测试
- `grill-me` / `grill-with-docs` — 若 PRD 开放问题 Q1/Q2（PG 凭证策略、构建期是否跑迁移）需要与用户对齐
- `handoff` — 会话再次接力时
